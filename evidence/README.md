# Frozen Evidence

The file `frozen_summary.json` is a machine-readable publication of the
aggregate values shown in the README and figures. It records the frozen
development and terminal experiment identifiers, input hashes, cost scenarios,
attribution, and interpretation flags.

The file contains aggregate evidence only. It does not include raw market data,
positions, private signals, configurations, or high-granularity run artifacts.

